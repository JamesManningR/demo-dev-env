# Simple MEVN Docker template

This project contains all of the boilerplate for a MEVN Stack project using docker. The API (express server) and the client (Vue project) are seperated into submodules.
